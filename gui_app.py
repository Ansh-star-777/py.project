from tkinter import *

window = Tk()

window.title("Length Converter App")

window.geometry("400x400")

label = Label(window, text="Length Converter", bg="lightblue")
label.pack(pady=20)

window.mainloop()
